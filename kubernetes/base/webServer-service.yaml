apiVersion: v1
kind: Service
metadata:
  name: webserver-service
  namespace: default
spec:
  selector:
    app: webserver
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
