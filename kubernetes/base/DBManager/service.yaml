apiVersion: v1
kind: Service
metadata:
  name: dbmanager-service
  namespace: default
spec:
  selector:
    app: dbmanager
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP