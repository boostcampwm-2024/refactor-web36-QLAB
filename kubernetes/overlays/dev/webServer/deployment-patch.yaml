apiVersion: apps/v1
kind: Deployment
metadata:
  name: webserver-deployment
spec:
  template:
    spec:
      containers:
      - name: webserver
        image: local-qlab-webserver:latest
        imagePullPolicy: Never