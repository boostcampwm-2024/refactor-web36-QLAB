apiVersion: apps/v1
kind: Deployment
metadata:
  name: apiserver-deployment
spec:
  template:
    spec:
      containers:
      - name: apiserver
        image: local-qlab-apiserver:latest
        imagePullPolicy: Never

