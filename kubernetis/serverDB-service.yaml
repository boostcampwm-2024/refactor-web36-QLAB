apiVersion: v1
kind: Service
metadata:
  name: serverdb-service
  namespace: default
spec:
  selector:
    app: serverdb
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
