apiVersion: v1
kind: Service
metadata:
  name: apiserver-service
  namespace: default
spec:
  selector:
    app: apiserver
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: LoadBalancer